name: CarteNabil-iOS-Build

on:
  push:
    branches: [ main, master ]
  workflow_dispatch:

jobs:
  build:
    runs-on: macos-latest

    steps:
      - uses: actions/checkout@v4

      - uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.24.3'

      - name: Clean & Rebuild iOS
        run: |
          flutter config --no-analytics
          rm -rf ios/
          flutter create --project-name=carte_nabil --platforms=ios .
          flutter pub get

      - name: Fix Deployment Target & Remove -G
        run: |
          # Force iOS 13.0 partout
          find ios -name "*.pbxproj" -exec sed -i '' 's/IPHONEOS_DEPLOYMENT_TARGET = [0-9.]*/IPHONEOS_DEPLOYMENT_TARGET = 13.0/g' {} +
          # Supprime le flag -G des fichiers de configuration
          find ios -type f -exec sed -i '' 's/-G//g' {} + || true

      - name: Add Camera Permission
        run: |
          /usr/libexec/PlistBuddy -c "Add :NSCameraUsageDescription string 'Scan cartes membres'" ios/Runner/Info.plist || true

      - name: Install Pods
        run: |
          cd ios
          pod install
          cd ..

      - name: Build iOS
        run: flutter build ios --release --no-codesign

      - name: Upload
        uses: actions/upload-artifact@v4
        with:
          name: CarteNabil-iOS-App
          path: build/ios/iphoneos/Runner.app
